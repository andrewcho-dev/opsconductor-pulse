# Prompt 004 — Nav + Routes

Read the operator router and nav in `frontend/src/`.

## Add Routes

- `/operator/users` → UserListPage
- `/operator/users/:userId` → UserDetailPage

## Add Nav Link

In operator sidebar: "Users" link → `/operator/users`
Place between "Tenants" and "Audit Log".

## Acceptance Criteria

- [ ] Both routes registered
- [ ] "Users" in operator nav
- [ ] `npm run build` passes
