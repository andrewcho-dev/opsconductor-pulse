# Task 2: Update Documentation

## Files to Update

1. `docs/development/frontend.md`
2. `docs/services/ui-iot.md`
3. `docs/features/device-management.md`
4. `docs/index.md`

---

## All Files

### Frontmatter (all four)

- Set `last-verified: 2026-02-20`
- Add `190` to the `phases` array

### Content Changes

**`docs/features/device-management.md`** — Update the Data tab description to note compact module layout:

```markdown
The Data tab displays expansion module slots in a compact single-table layout (one row per slot with inline assign buttons), followed by the sensors table with CRUD operations, and telemetry time-series charts.
```

**Other files** — No content changes, frontmatter only.

## Verification

- All four docs have `190` in their `phases` array
- `last-verified` dates updated
