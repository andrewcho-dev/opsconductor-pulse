# MQTT password file — generated by mosquitto_passwd
# DO NOT commit the real passwd file.
# Generate with:
#   mosquitto_passwd -c -b passwd "service_pulse" "${MQTT_ADMIN_PASSWORD}"
#   mosquitto_passwd -b passwd "device_{tenant_id}_{device_id}" "${DEVICE_PASSWORD}"
