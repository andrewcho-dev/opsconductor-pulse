# =============================================================
# OpsConductor Pulse — Mosquitto ACL
# =============================================================
# NOTE: With allow_anonymous=true, these ACLs apply to ALL
# anonymous clients. True per-tenant isolation requires adding
# authentication (password_file or client certificates) where
# each tenant's credentials map to their topic pattern.
# =============================================================

# Device telemetry ingestion (existing)
topic readwrite tenant/+/device/+/+

# Alert delivery — publish and subscribe
topic write alerts/#
topic read alerts/#
