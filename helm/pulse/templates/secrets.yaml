apiVersion: v1
kind: Secret
metadata:
  name: pulse-db-secret
  labels:
    {{- include "pulse.labels" . | nindent 4 }}
type: Opaque
stringData:
  # For Bitnami postgresql (existingSecret) and app DATABASE_URL (url)
  password: "change-me"
  url: "postgresql://iot:change-me@pulse-postgresql:5432/iotcloud"

